import { BundleOptions } from "@swc/core/spack";
export interface SpackCliOptions {
    debug: boolean;
}
export default function parseSpackArgs(args: string[]): Promise<{
    cliOptions: SpackCliOptions;
    spackOptions: BundleOptions;
}>;
